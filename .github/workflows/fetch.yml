name: Fetch NPV Group Configs
on:
  schedule:
    - cron: "*/20 * * * *"  # هر 20 دقیقه (بر اساس UTC)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install telethon

      - name: Fetch configs from group
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          SESSION: ${{ secrets.SESSION }}
        run: python fetch_configs.py

      - name: Add timestamp (even if no change)
        run: |
          echo "# Updated at $(date)" >> configs.txt
          git add configs.txt

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "auto update configs" && git push
